{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd apps/backend && prisma generate && tsc && echo '=== Build verification ===' && ls -la dist/ && test -f dist/server.js && echo 'server.js exists' || echo 'server.js NOT FOUND'"
  },
  "deploy": {
    "startCommand": "cd apps/backend && pwd && ls -la && ls -la dist/ && node dist/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

