{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm --filter @side-project/backend build && echo '=== Verifying build output ===' && ls -la apps/backend/dist/ && find apps/backend -name 'server.js' -type f"
  },
  "deploy": {
    "startCommand": "cd apps/backend && node dist/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

