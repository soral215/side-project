[phases.setup]
nixPkgs = ["nodejs-20_x", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = [
  "cd apps/backend",
  "prisma generate",
  "tsc"
]

[start]
cmd = "cd apps/backend && node dist/server.js"

